#include <bits/stdc++.h>
using namespace std;
// #define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  double D, T, S; cin >> D >> T >> S;
  if((D / S) <= T) cout << "Yes" << '\n';
  else cout << "No" << '\n';
}